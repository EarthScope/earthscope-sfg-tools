name: Build Go Binaries

on:
  push:
    branches: [main]
    paths:
      - 'src/go/**'
      - '.github/workflows/build-go.yml'

  pull_request:
    branches: [main]
    paths:
      - 'src/go/**'
      - '.github/workflows/build-go.yml'

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            pixi-env: tiledb
          - os: macos-latest   # arm64
            pixi-env: tiledb
          - os: windows-latest
            pixi-env: tiledb
            
    runs-on: ${{ matrix.os }}

    steps:
      - uses: actions/checkout@v4

      - name: Set up pixi
        uses: prefix-dev/setup-pixi@v0.8.3
        with:
          environments: ${{ matrix.pixi-env }}
          locked: false

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: src/go/go.mod
          cache-dependency-path: src/go/go.sum


      - name: Build Go binary (Unix)
        if: runner.os != 'Windows'
        run: pixi run -e ${{ matrix.pixi-env }} build-go

      # The Makefile is Unix-only (make + shell conditionals) and conda-on-Windows
      # lays headers/libs out under Library/{include,lib} rather than the Unix
      # {include,lib}, so Windows gets its own build step instead of `make`.
      # pixi's win-64 `go-cgo` dependency pulls in a MinGW-w64 gcc that links fine
      # against conda-forge's (MSVC-built) tiledb win-64 package -- verified below
      # by actually running the resulting exe, not just checking that link succeeded.
      - name: Build Go binary (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          set -euxo pipefail
          PREFIX="$(pwd)/.pixi/envs/${{ matrix.pixi-env }}/Library"
          mkdir -p src/go/build
          cd src/go
          pixi run -e ${{ matrix.pixi-env }} bash -c "
            set -euxo pipefail
            CGO_ENABLED=1 GOOS=windows GOARCH=amd64 \
            CGO_CFLAGS='-I${PREFIX}/include' \
            CGO_LDFLAGS='-L${PREFIX}/lib -ltiledb' \
            go build -o build/sfg_windows_amd64.exe -buildvcs=false ./cmd
            PATH=\"${PREFIX}/bin:\$PATH\" ./build/sfg_windows_amd64.exe --help >/dev/null
          "
  commit-binaries:
    needs: build
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download all platform binaries
        uses: actions/download-artifact@v4
        with:
          pattern: go-binary-*
          path: src/go/build/
          merge-multiple: true

      - name: Make binaries executable
        run: chmod +x src/go/build/sfg_*

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/go/build/sfg_*
          git diff --staged --quiet && echo "No binary changes." || (
            git commit -m "chore: update pre-built Go binaries [skip ci]" &&
            git push
          )