version: 2

build:
  os: ubuntu-24.04
  tools:
    python: "3.12"
    nodejs: "20"
  commands:
    - npm install -g mystmd
    - myst build --html
    - mkdir -p $READTHEDOCS_OUTPUT/html
    - cp -r _build/html/. $READTHEDOCS_OUTPUT/html
